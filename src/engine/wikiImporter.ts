// src/engine/wikiImporter.ts
// REMOVED: this module previously called the Anthropic API to parse pasted
// text / wiki URLs into homebrew content. The app no longer includes any AI
// features. Homebrew content is authored entirely through the guided builders.
//
// This file is intentionally empty (no imports, no network calls) and is safe
// to delete. It is kept only so any lingering import resolves rather than
// throwing a missing-module error.

export {};
