import { makeEmptyEntity, DEFAULT_RULES } from '../../../store/characterStore';import { makeCustomRuleProfile } from '../../../engine/customRuleProfiles';import { asRulesetId } from '../../../engine/types';import { characterHeaderUiModel, freeEditUiModel } from '../sheetUiModel';
describe('sheet UI integration pass 5',()=>{const e={...makeEmptyEntity('e'),rulesetId:asRulesetId('dnd5e-2014')};const free=makeCustomRuleProfile({id:'free',name:'Free',baseRulesetId:e.rulesetId!,rules:{customRules:{lockPlayerFreeEdit:false}}});const locked={...free,id:'locked',rules:{customRules:{lockPlayerFreeEdit:true}}};
it('reports locked versus nonempty allowed sections',()=>{expect(freeEditUiModel({...DEFAULT_RULES,customRules:{lockPlayerFreeEdit:true}},e,[])).toMatchObject({locked:true,sections:[],hasControls:false});const vm=freeEditUiModel(DEFAULT_RULES,e,[]);expect(vm.locked).toBe(false);expect(vm.hasControls).toBe(true);expect(vm.sections.length).toBeGreaterThan(0)});
it('keeps controls for a standalone character with manual overrides',()=>{const entity={...e,characterOverrides:[{id:'m',entityId:e.id,stat:'ac',operation:'set' as const,value:18,label:'Manual',active:true,appliedAt:1,cancelledAt:null}]};expect(freeEditUiModel(DEFAULT_RULES,entity,[]).sections).toContain('Manual Overrides')});
it('updates live from custom profile lock changes',()=>{const entity={...e,customRuleProfileId:'free'};expect(freeEditUiModel(DEFAULT_RULES,entity,[free]).locked).toBe(false);expect(freeEditUiModel(DEFAULT_RULES,entity,[{...locked,id:'free'}]).locked).toBe(true);expect(freeEditUiModel(DEFAULT_RULES,entity,[free]).locked).toBe(false)});
it('prioritizes compact header actions and preserves overflow permissions',()=>{const vm=characterHeaderUiModel({canFreeEdit:false,canUndo:false,canRedo:true,hasDiagnostics:true});expect(vm.direct).toEqual([{id:'undo',enabled:false},{id:'redo',enabled:true},{id:'more',enabled:true}]);expect(vm.overflow.find(x=>x.id==='rules')?.enabled).toBe(true);expect(vm.overflow.find(x=>x.id==='edit')?.enabled).toBe(false);expect(vm.overflow.map(x=>x.id)).toEqual(['history','rules','edit','export','diagnostics'])});});
